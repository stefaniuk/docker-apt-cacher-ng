machine:
  services:
    - docker
dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build --tag stefaniuk/debian .
    - mkdir -p ~/docker; docker save stefaniuk/debian > ~/docker/image.tar
test:
  override:
    - :
